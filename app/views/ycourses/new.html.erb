<div class="video-url text-center">

  <%= simple_form_for @ycourse do |f| %>
  <% if @ycourse.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@ycourse.errors.count, "error") %> prohibited this course from being saved:</h2>

        <ul>
          <% @ycourse.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <%= f.input :url, placeholder: 'Please enter an URL from Youtube', input_html: { class: "url-input form-control-lg" }, label:'' %>
    <%=  f.button :submit, 'Insert a course!', class: 'button btn-url color-white w-button pl-6 pr-6' %>
  <% end %>

</div>